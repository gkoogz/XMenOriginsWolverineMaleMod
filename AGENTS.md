# Working across accounts

Read `docs/PROJECT_HANDOFF.md` at the start of work. Keep it current after
material discoveries, changes, tests, installation, and user feedback so the
other agent can resume without session memory. Distinguish observations,
hypotheses, proposals, compiled candidates, and user-confirmed results.

This repository is the v0.6 / character161 / runtime161 release. The v0.5 tag preserves runtime157. Experimental development
is in the shared workspace's `work/hd`; do not confuse newer experiment
numbers with approved releases. See the handoff for paths and invariants.
