# Lower shelf and softer terminal convergence — R8

R8 reduces the dorsal reference shelf by up to 0.23 authoring units and carries that reduced profile around the underside. The ventral sweep endpoint advances from parameter 5.10 to 5.65; its contribution decreases to zero at the centerline. This couples the construction's wall height and convergence instead of adding another rim or groove. A local profile-guide correction, shape-preserving terminal meridians and limited arc-length-weighted fairing soften the pre-tip transition. The slit remains an external recess.

The result remains a rounded blended surface. Under cavity lighting, a faint curved transition remains before the slit; this is not a mathematically sharp V. User review of the convergence and side silhouette is still needed. No claim of clinical accuracy is made.

R7 length is retained within 0.001 authoring units. Same topology: 10,554 vertices / 21,086 triangles. Geometry checks pass at 41 sizes; no edited open/nonmanifold edges or sampled nonadjacent intersections. Five BVH sizes pass. A 96-meridian radial audit finds at most one significant peak per meridian (prominence 0.08). See change-audit.json for actual profile-guide measurements; guide correction alone is not an exact straight-line constraint after terminal fitting.

Comparison uses the actual R7 Blender model. Five sizes, four views, three shading styles; 120 matched renders. Size mapping remains 0=0.85x, 50=1.00x, 100=1.60x. Blender control is continuous; OBJ is size 50. Preserve custom normals: the fitted shaft band is static, while the crown uses automatic normals. Source scripts require existing workspace inputs.

Authoring preview only. Previous outputs retained. No game launch, installation or game-file edits. UV/rig transfer, physics and runtime integration are not included.
